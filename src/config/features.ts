/**
 * Drapeaux de fonctionnalités. Un drapeau éteint ne charge aucun code lié.
 */
export const FEATURES = {
  /** Publicités interstitielles en fin de session d'unité. SDK non installé en v1. */
  ads: false,
} as const;
